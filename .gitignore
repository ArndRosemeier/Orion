node_modules
dist
dist-ssr
*.local
.env
.env.*.local
.DS_Store
coverage
*.log
.vite

# Rust build output (the committed artifact lives in src/domain/engines/l0.wasm)
crates/*/target

# Playwright artifacts
test-results/
playwright-report/

# Machine-local browser environment written by scripts/setup-browser.sh
.browser-env.sh

# Python bytecode from the deploy tooling
__pycache__/
*.pyc

# Deploy backups (the registry as downloaded before each write)
.futuremagic/
.ftp.env.local
