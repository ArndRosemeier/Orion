# Decision ledger

**Append-only.** One row per decision. Never edit another landing's row.
Behaviour lives in tests; this file holds _why_, evidence, rejected options, and what is unproven.

| n   | decision                                                                                                                                                                                                                                                                                                                                                                     | files                                                                       | why / evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | status   |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| 1   | Day-1 stack: Vite + React + TypeScript 5.9 + Vitest + Zod; package manager pnpm (forbid npm/yarn/bun)                                                                                                                                                                                                                                                                        | `package.json`, `AGENTS.md`, `vite.config.ts`, `eslint.config.js`           | Opinionated until Arnd redirects; pnpm present on box; TS pinned to 5.9 (typescript-eslint does not support TS 7 yet); Zod for env/boundary SSOT                                                                                                                                                                                                                                                                                                                                                                                                                                                            | accepted |
| 2   | **Product direction:** fractal explorer with arbitrarily deep zoom that always uses the cheapest sufficient math for the current zoom stage (a _precision ladder_), GPU-first behind ONE backend seam (WebGPU + WebGL2), and a multithreaded CPU path that is the **primary** engine at extreme depth rather than a mere fallback                                            | `docs/ARCHITECTURE.md`, `AGENTS.md`                                         | Arnd directive 2026-09-17; supersedes "product features TBD" in `AGENTS.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | accepted |
| 3   | **Verification strategy for GPU code on a GPU-less box:** headless Chrome + Chrome's bundled SwiftShader software rasterizer. WebGL2 confirmed working; WebGPU confirmed (adapter **and** device). Performance is explicitly _not_ verifiable here — SwiftShader is 100–1000× slower than silicon, so frame timings come from Arnd's GPU via an in-app HUD + benchmark route | `docs/TESTING.md`                                                           | Probe 2026-09-17, Chrome 151.0.7922.169: WebGL2 renderer `ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (Subzero)))`, `EXT_color_buffer_float: true`; WebGPU adapter+device with `--enable-unsafe-webgpu --enable-features=Vulkan --use-vulkan=swiftshader`. Box has no `/dev/dri`                                                                                                                                                                                                                                                                                                                        | accepted |
| 4   | **Approved new deps:** `playwright@1.63.0` (dev; headless GPU differential + golden pins; browser download skipped, drives the system Chrome) and the Rust→WASM toolchain (rustup 1.29.1, rustc 1.98.1, `wasm32-unknown-unknown`, installed into `~/.cargo` under an approved sandbox escalation)                                                                            | `package.json`, `pnpm-lock.yaml`                                            | Arnd approval 2026-09-17. Windows/CI note: `~/.cargo/bin` is **not** on the default PATH — build scripts must invoke it explicitly rather than assume `cargo` resolves                                                                                                                                                                                                                                                                                                                                                                                                                                      | accepted |
| 5   | **Precision substrate:** `BigFixed` arbitrary-precision binary fixed point with ONE rounding rule (half away from zero), no silent rescaling (mixed precision throws), and working precision derived from the **scale exponent** rather than from the view                                                                                                                   | `src/domain/numeric/bigfixed.ts`, `precision.ts`, `src/domain/view/view.ts` | Deriving precision from a view is circular — a view built too coarse cannot report the precision it needs, so `makeView` _validates_ instead. Rejected: float round-trip derivation (loses the last bit exactly where it matters — pinned by `fracBitsForPixelSize(3·2^-1000)`)                                                                                                                                                                                                                                                                                                                             | accepted |
| 6   | **Floatexp storage:** a real value carried as `(mantissa, unbounded integer exponent)`, mantissa normalised to `[0.5, 1)`. It is a _wider-range_ type, not a higher-precision one — still 53 significant bits.                                                                                                                                                               | `src/domain/numeric/floatexp.ts`                                            | Reference orbits legitimately hold values ~2^-2000 and the delta iteration goes lower; a plain double underflows to zero there and silently degrades a valid orbit. Rejected: a single double (underflows), arbitrary precision per orbit value (the per-pixel engine only consumes 53 bits, so the extra width is paid for and thrown away)                                                                                                                                                                                                                                                                | accepted |
| 7   | **Reference orbit: computed at full precision, stored at 53 bits.** The per-pixel offset `dc` is computed at full precision and carries the zoom; the stored orbit only supplies the linear coefficient.                                                                                                                                                                     | `src/domain/engines/reference.ts`                                           | The orbit must be computed at high precision because each step amplifies error by ~`\|2Z_n\|` — a 53-bit computation _is a different orbit_. Storage may be coarse because rounding enters as a bounded additive perturbation of a macroscopic escape test (radius 2), not as a shifted starting point. Measured, not assumed: for `-2 + 2^-200`, 512 bits holds the fixed point for ~50 iterations and departs by ~150 while 64 bits rounds to `-2` and stays forever — **precision buys iterations**. Storing the offset in the orbit instead of the delta is the failure mode this row exists to prevent | accepted |

| 8 | **Perturbation engine with explicit glitch repair.** `d_{n+1} = 2Z_n d_n + d_n^2 + dc`; a glitched pixel is returned as a distinct union case and repaired by the direct engine, never guessed at or clamped. Criterion: `\|z\|^2 < \|Z\|^2 * 2^-24`. | `src/domain/engines/perturbation.ts`, `src/domain/numeric/floatcomplex.ts` | Escape iteration counts pinned **equal to the direct oracle** across 5 views including 3000-iteration orbits, with zero undetected glitches. Measured error mechanism: the delta's accumulated error reaches 6.2e-6 relative at n=1531, in the regime where `\|d\| > \|Z\|` — it perturbs the sub-iteration smooth count but never the escape iteration. Rejected: clamping or silently accepting glitched pixels (produces a plausible wrong image); repair via a second reference orbit (deferred until the ladder needs it) | accepted |
| 9 | **Decimal coordinates parse straight to fixed point** (`fromDecimal`), with no exponent notation and no separators. | `src/domain/numeric/bigfixed.ts` | Deep-zoom coordinates arrive as decimal strings — shared URLs, saved locations, published references. Routing them through a double discards every digit past the 53rd bit, which is precisely the part that defines a deep view; pinned by comparing a 33-digit coordinate against its double projection | accepted |

| 10 | **Series approximation: the skip point is validated, never extrapolated.** Coefficients come from the standard power-series recurrence; the largest iteration whose prediction matches the exact delta on sampled offsets, within tolerance of the leading term, is the one used. | `src/domain/engines/series.ts` | Coefficients pinned against `exactDelta` to ~1e-17 relative. The first implementation bounded the tail by assuming geometric coefficient decay; measurement showed the coefficients _grow_ with order and the estimate under-reported the true error by 20+ orders of magnitude, with no safety factor able to repair a bound of the wrong shape. Validation replaced it: 0 bound violations across every pixel of the pinned views, worst ratio ~0.23 (4–18× conservative) | accepted |
| 11 | **The series path is an opt-in accelerator, not the correctness baseline.** `escapePerturbedWithSeries` reports `skipped`, and the exact perturbation path remains the default. | `src/domain/engines/series.ts` | Measured: seeding the recurrence from a truncated series rather than iterating from `d_1 = dc` changes the escape count for near-boundary pixels — 8/36 at tolerance 2^-24, still 1/36 at 2^-53, with the same view's exact path matching the oracle on 36/36. The seed determines which trajectory the pixel follows, and near the boundary that is the image content. Pinned as a known limitation with both bounds as tripwires; the ladder (S1e) must gate the accelerator per view | accepted |

| 12 | **Orbit precision is verified, not derived.** Compute at `F` and `2F` from identical input, compare the _stored_ 53-bit values, and double until they agree; fail loudly at the cap. Never start below the input's own precision. | `src/domain/engines/reference.ts` | The worst-case bound (`53 + 2n`, i.e. two bits per iteration of amplification) is far too pessimistic to budget with: measured, a 1500-iteration orbit of the deep seahorse coordinate at **256 bits** agrees with 512 bits, and only a maximally-amplifying point (`-2 + 2^-200`) actually forces a raise. A formula tight enough to be useful would be a guess; agreement is checkable. Caveat with its own pin: computing below the input precision silently degrades the coordinate and the comparison cannot notice, because both sides agree on the degraded point | accepted |
| 13 | **The ladder decides by validity first, cost second, and gates the series accelerator on an explicit `quality`.** `exact` never selects the series stage; `preview` may. Every option is reported with its reason and its work estimate. | `src/domain/ladder/plan.ts` | A stage that cannot express the view is never chosen however cheap it looks (`direct-f64` below `2^-52` cannot even represent a pixel offset). The cost model is deliberately **structural, not calibrated** — this box has no GPU, so nanosecond figures would be fiction; work is counted in bigint-iteration equivalents with a limb factor, comparable between stages and against a real HUD later. Grounded in ledger row 11: an approximation must never be selectable when exact output was asked for | accepted |

| 14 | **Rendering is GPU-first behind ONE backend seam, and the GPU L0 path is a _preview_ engine.** `FractalBackend` is the only interface; callers choose by capability, never by name. `exact` quality routes to the CPU backend, for the same reason ledger row 11 gates the series accelerator. | `src/domain/render/backend.ts`, `webgl2.ts`, `cpu.ts` | Measured in the browser lane: the f32 shader agrees with the f64 CPU engine on **every one of 3072 pixels at an 8-iteration budget** — which is what proves the coordinate mapping, y-flip, origin and readback are right — and differs on 328 of 3072 (10.7%) at 400 iterations, 89 of those by more than 50. That is f32 rounding accumulating until trajectories diverge, not a defect, so the shader is offered for `preview` and refuses `exact` with the measurement as its reason | accepted |
| 15 | **The browser lane runs inside the one gate** (`scripts/gate.sh` → typecheck, lint, test, test:browser), driving the real Vite server and the real backends through headless Chrome + SwiftShader. Correctness only; nothing in it may assert a timing. | `playwright.config.ts`, `tests/browser/`, `scripts/gate.sh` | SwiftShader is the only rasteriser on this box (round-1 probe), so it can prove agreement and nothing about speed. Escape counts cross the boundary as numbers via a float framebuffer, so the comparison is numeric rather than a colour heuristic. Rejected: a separate opt-in lane, which would let a landing claim "done" without ever running the GPU pins | accepted |

| 16 | **Rendering is progressive and cancellable, in that order of importance.** A coarse pass at 1/8 resolution lands first, the full pass follows, and an `AbortSignal` stops the run between tiles. Tiles are cut in _sample_ space and clipped so that every sample is written exactly once. | `src/domain/render/scheduler.ts`, `tiles.ts` | Pinned: exact coverage per pass (no gap, no overlap) across 8 view/step/tile geometries; the coarse image is 13x9 for a 100x70 view where the full is 100x70; cancellation returns `cancelled: true` with the partial work it finished and never a completed pass it did not finish. Cancellation is a normal outcome of panning, so it is a return value rather than an exception — but a backend that cannot render the view at all still throws with its reason | accepted |
| 17 | **The reference point is the view centre, and the orbit cache is keyed on it.** | `src/domain/render/cpu.ts`, `tiles.ts` | Per-tile reference points would give each tile its own expensive high-precision setup and make caching useless. With one reference per view, four tiles of a deep view produce **1 cache miss and 3 hits** (pinned), so the setup runs once per view instead of once per tile. The cache is a bounded LRU with recency on read, evicting the least recently _used_ rather than the oldest written (pinned) | accepted |

| 18 | **The second GPU backend lands behind the same seam, judged by the same differential.** A WGSL compute kernel iterating the identical recurrence, reachable by name from the harness so both GPU backends are held to one reference. | `src/domain/render/webgpu.ts`, `webgpu-types.ts` | Measured: WebGPU agrees with the f64 CPU engine on **all 3072 pixels at an 8-iteration budget** and stays inside the same divergence bounds at 400 — so the seam is a fact, not a design, and the two kernels agree with each other through the oracle. WebGPU types are declared locally rather than adding `@webgpu/types`, and the browser lane is what validates the declarations against the real API. Chrome needs `--enable-unsafe-webgpu --enable-features=Vulkan --use-vulkan=swiftshader` for a headless adapter | accepted |
| 19 | **Backend selection is GPU-first and never silent.** `chooseBackend` tries candidates in the caller's order and returns _every_ refusal reason when none can render, rather than one collapsed message. | `src/domain/render/select.ts` | Pinned: the first capable candidate wins and the refusals it skipped are reported; a later candidate is chosen when an earlier one refuses, with the reason kept; an empty candidate list is a loud error. The app names its chosen backend in the status line, and the browser smoke test asserts `webgpu · …`, so a silent downgrade to WebGL2 would fail the gate | accepted |

| 20 | **Emulated double precision in the GPU kernel, selected by `quality`.** `exact` always takes the `(hi, lo)` kernel (~48 significant bits via error-free transformations and `fma`); `preview` takes plain `f32`. | `src/domain/render/webgpu-shader.ts`, `webgpu.ts` | Measured: the double kernel reproduces the **arbitrary-precision oracle exactly** on a 2^-24 view, where plain `f32` cannot represent the view at all and the capability now refuses preview outright. It also agrees exactly with the f64 engine at a low budget. `exact` never falls back to single: f32 diverges from the oracle as iterations accumulate even at shallow depths, so there is no depth at which it is "exact enough" to promise. Cost is ~10x arithmetic per iteration, which is why it is a separate kernel rather than a replacement | accepted |
| 21 | **WebGPU shader compilation and dispatch are checked, never assumed.** `getCompilationInfo()` is inspected after module creation, and every dispatch runs inside a validation error scope whose result is raised. | `src/domain/render/webgpu.ts` | A WGSL error or a binding mismatch does not throw: the pipeline is inert, the dispatch becomes a no-op, and the output buffer keeps stale bytes that look exactly like a numerical result. Found the hard way — the first double-precision run returned 3072/3072 "wrong" values with no error at all, which is the silent wrong answer this project exists to refuse | accepted |

| 22 | **Shareable links are bit-exact by default, with a decimal form for humans.** Two explicit encodings selected by a `fmt` field: `e` writes each coordinate's fixed-point integer in base 36 (a 256-bit coordinate is ~52 characters, round trip exact); `d` is plain decimal parsed by `fromDecimal`, for a link someone typed. Every field is Zod-validated before use, and `makeView` still runs afterwards. | `src/domain/view/url.ts`, `src/domain/numeric/bigfixed.ts` | Pinned: bit-exact round trip of a 33-digit deep coordinate and of a 256-bit dyadic centre, every pixel of a decoded view mapping to the same complex point, a fragment under 220 characters, and a malformed link failing **loudly** with its reason. Rejected: decimal-only (a deep centre needs hundreds of digits, and a link that silently rounds would open the wrong place); base36-only (a human cannot edit one). Also rejects a link whose declared precision cannot resolve its own pixel grid | accepted |
| 23 | **`toDecimalString` is exact, not rounded** — a fixed-point value is dyadic, so its decimal expansion is finite and can be written exactly. | `src/domain/numeric/bigfixed.ts` | The fractional part is `n/2^F == n*5^F/10^F`, so the digits are one bigint multiplication rather than a digit loop, and there is no rounding error to apologise for. Pinned by round-tripping random doubles and a 33-digit coordinate through `toDecimalString` → `fromDecimal` with a bit-exact comparison | accepted |

| 24 | **The CPU path runs in a worker pool that implements the same backend interface.** `createWorkerPool` returns a `FractalBackend`, so `chooseBackend` offers it as a candidate and the scheduler dispatches to it unchanged; nothing downstream knows it exists. The view crosses the worker boundary as a **URL fragment**, reusing the shareable-link codec. | `src/domain/render/workerPool.ts`, `src/worker/tile.worker.ts`, `scheduler.ts` | The claim is bit-identical output, and it is pinned: a 32x24 view rendered as **12 tiles across 4 workers** produces exactly the image the single-threaded backend produces as one tile — which also proves the scheduler places every tile correctly, since a gap or overlap would show as a mismatch. Workers run the _same_ CPU backend, not a copy. Rejected: rendering on the main thread when workers are unavailable (a different performance profile than the caller asked for) — the pool throws with that reason instead | accepted |
| 25 | **Cross-origin isolation is asserted, never assumed.** `vite.config.ts` sends COOP/COEP so `SharedArrayBuffer` exists; the pool **reports** `transport: "shared" | "transfer"` and the browser lane asserts the isolated case is the one running. | `vite.config.ts`, `workerPool.ts` | A missing header would otherwise make the pool quietly use a different transport — a silent behaviour change. A static host must send the same two headers, and the failure is visible in the reported transport rather than in a performance mystery. A worker writes a tile straight into a shared buffer with no copy or clone; the remaining copy into the caller's result is recorded as the next step (writing directly into a shared pass image) | accepted |

| 26 | **The L0 recurrence gains a Rust→WASM SIMD carrier.** `no_std`, no allocator, no dependencies, ~1.3 KB: it renders a row of pixels into two fixed static buffers and the host reads them. `f64x2` pairs two pixels at a time. Loaded lazily; the stage string reports which carrier ran. | `crates/l0/`, `src/domain/engines/l0Wasm.ts`, `directFloat.ts` | Measured in the browser lane: **bit-identical** to the JavaScript kernel over 2048 pixels at 400 iterations (0 mismatches) and **1.4-1.9x faster across runs** (measured samples: 1.966ms → 1.054ms, and 2.310ms → 1.683ms). Bit-identity is achievable because both were written to the same order of operations, and it is the property that makes the speedup safe: a carrier swap cannot move a pixel. Rejected: `fma` (more accurate, and would break equality with the pinned reference); `log2` in Rust (the host computes the smooth count so both carriers use the same libm) | accepted |
| 27 | **The committed `.wasm` is checked against its source in the gate.** `scripts/check-wasm.sh` rebuilds `crates/l0` and compares bytes; a mismatch fails with the rebuild command. | `scripts/check-wasm.sh`, `scripts/gate.sh` | A checked-in binary that can drift from its source is a silent divergence — the tests would keep passing against a stale kernel. Verified by appending one byte to the artifact: the check fails with an actionable message, and passes again once rebuilt. This makes the gate depend on the Rust toolchain, which is now a real dependency of the project rather than an optional extra | accepted |

| 28 | **The series accelerator is wired into the CPU backend's `preview` path, with its coefficients cached beside the reference orbit.** `exact` never takes it. | `src/domain/render/cpu.ts` | The accelerator had been implemented and pinned since landing 5 but **no renderer called it** — a whole landing's work unused. Now: `exact` reports stage `perturbation`, `preview` reports `perturbation+series`, and coefficients are built once per view (pinned: **1 miss, 3 hits** across four tiles, exactly like the orbit). Measured on the perturbation stage at 2^-56 with 3000 iterations: **1.3-1.5x across runs** (samples: 89.5ms → 59.6ms, and 74.8ms → 56.2ms); a second view measured 2.15x. Both views are uniformly interior at their budget, so the figure speaks to skipped iterations rather than to a mixed image | accepted |

| 29 | **Tiles live on an absolute _sample_ lattice, so panning reuses them.** The view is snapped so that sample `j` sits at `j * sampleSize` in complex space; the cache key is `(spacing, fracBits, step, samplesAcross, k, l)` with `(k, l)` the absolute tile index. Viewport-cut tiles cannot be reused at all after a pan, because what a tile contains depends on where the viewport happened to fall. | `src/domain/render/tiles.ts`, `scheduler.ts`, `backend.ts` | Snapping moves the image by at most half a sample — below the resolution of the pass being rendered, by construction — and that is the price of reuse. Pinned against an **analytic oracle** (every pixel recomputed from the view with no tiles, no cache, no scheduler) rather than against a second cache run: cold and warm renders must both reproduce the oracle exactly, while a pan must report reuse. A warm-vs-cold comparison alone was **inert** — it passed a mutation that dropped the row index from the key, because both sides were then wrong the same way. With the oracle, five separate mutations die: key without `k`, key without `l`, snapping off by one sample, blit reading the wrong source row, blit shifted by one pixel. Measured in the browser lane through the real CPU backend: a 3-pixel pan reuses tiles (`warmSkipped > 0`, fewer tiles rendered) and the warm image is bit-identical to a cold one | accepted |

| 30 | **Navigation happens at the precision the _new_ scale needs, and the app's view state is fixed point.** `zoomAtPixel` / `panByPixels` derive the working precision from the target scale (never from the view being moved), raise the view to it exactly, and anchor through the exact inverse of `pixelToComplex`. Every deep link is kept exactly as decoded. | `src/domain/view/navigate.ts`, `view.ts`, `label.ts`, `src/App.tsx` | The engine could already render 2^-400; the _app_ could not name such a place, because its centre was a `double` — so the product silently stopped going deeper while every renderer was capable. Now pinned: 400 zoom steps reach 2^-400 with the anchored point drifting < 10^-3 px; the ladder refuses direct-f64 by name at that depth and picks `perturbation`; the link the app writes round-trips bit-exactly (a 2^-173 width keeps ~52 significant decimal digits). Rejected: keeping the centre in doubles and only making the _renderer_ precise (the explorer would still stop), and re-deriving precision inside the view (circular — a view too coarse cannot report what it needs). Honest cost: a navigation step is _rounded_, so the anchor moves by a few units in the last place; that is measured and pinned as a fraction of a pixel, not claimed to be zero | accepted |

| 31 | **A WebGPU render waits for the previous readback before touching the shared buffers.** The backend's buffers (staging, counts, colours) belong to the backend, not to a render, and a staging buffer can be mapped only once at a time. | `src/domain/render/webgpu.ts` | Found by the app-level zoom pin, not by reasoning: six wheel events in quick succession — the normal gesture — made the second render submit a copy into a staging buffer whose `mapAsync` was still pending, and WebGPU rejected it (`used in submit while pending map`). The worse half is silent: without the queue the second render could overwrite the buffer mid-read and return the _previous_ frame's bytes as its own, which is precisely the silent wrong answer this project exists to refuse. The app showed the error rather than a wrong image, because every dispatch already runs inside a validation error scope (row 21). Pinned by mutation: removing the wait brings the failure straight back | accepted |

| 32 | **The delta recurrence gets a Rust→WASM carrier, held to the JavaScript engine bit for bit.** Same loop, same order of operations, `no_std`, no allocator, ~7.9 KB. The reference orbit and the per-pixel start state (which iteration, from which delta) are uploaded into fixed statics; the kernel iterates a whole tile per call. | `crates/perturb/`, `src/domain/engines/perturbWasm.ts`, `perturbation.ts` (`DeltaCarrier`), `cpu.ts`, `scripts/{build,check}-wasm.sh` | **Measured first, and it redirected the work**: the delta loop is ~100% of a deep frame's cost — 700 microseconds per pixel at a 2^-100 view, ~1.2 microseconds per _iteration_, because Floatexp arithmetic in JavaScript allocates a `{m, e}` object per operation and normalises through a `DataView`. The kernel does the same arithmetic in registers: **3.31x** on a real 2^-199 view (768 px x 300 iterations, orbit 301 values at 1024 bits: 199.6ms → 60.3ms), bit-identical. Rejected: a tolerance-based comparison (it would hide exactly the off-by-one and rounding differences a carrier swap introduces); `fma` and any reordering (more accurate, and a different function); `log2` in Rust (the smooth count stays with the host so both carriers use the same libm). The `DeltaCarrier` seam is what makes the swap a one-line choice and keeps both implementations fed by the identical plan — including the series path's skipped prefix, which starts the loop at iteration `skip` from a delta that is _not_ the offset | accepted |

| 33 | **A pass is a unit of work, so its tiling is chosen by the backend that does the work.** `passesFor(kind, width, height, workers)` gives a GPU the single-draw tiles it wants and gives a worker pool enough tiles that every worker has several waves. | `src/domain/render/passes.ts`, `App.tsx`, `select.ts` (`backendKind`) | One tile means one _worker_: the app's coarse pass (120x80 samples at step 8, 600 iterations, four workers) was a single 1024-sample tile, so the pass that exists to put something on screen quickly ran on one of eight workers. Measured: **one tile 1735ms, planner's tiling 612ms — 2.8x**, and 1332ms vs 607ms in an earlier run at 32 samples. Rejected: one global constant (that is what was there, and it was tuned for the GPU); a per-backend _time_ budget (unmeasurable on the GPU here, so it would be a guess). The tile size is bounded on both sides by measurement: too few tiles idles workers, too many pays per-tile overhead (~2.5ms measured, and 8x8 tiles cost 2.5x more per pixel than 32x32) | accepted |

| 34 | **Rebasing is deliberately NOT implemented, and the reason is a measurement.** The objective names "floatexp deltas with rebasing"; the technique exists in Kalles Fraktaler to stop a reference orbit's accumulated _absolute_ error from invalidating the deltas, and to avoid the glitch bands that follow. This codebase stores the orbit as `Floatexp` (53-bit mantissa, unbounded exponent) computed at a _converged_ precision, and detects cancellation with Pauldelbrot's criterion, repairing exactly by direct iteration — so the premise the technique addresses is already handled, and there is nothing left to rebase. | measured, no new code | Across **10 views x 3072 pixels, up to 3000 iterations** (interior regions at 2^-6…2^-300, mini-set and spiral neighbourhoods, escaping edges): **zero** glitches and zero orbit exhaustions. The glitch rate is governed by iteration-budget-versus-depth — a delta starts at the pixel scale and grows about one bit per iteration, and interior orbits have negative Lyapunov exponent, so deltas decay rather than diverge. **Caveat, stated because the first control was wrong:** the control case (c = -1, whose orbit hits 0 exactly) initially reported 0% because the reference is the _view centre_, which lands half a pixel away from -1 and gives `Z_2 ~ 2^-67`, dominating the deltas. The detector is therefore not yet _proven_ to fire in this codebase; what is proven is that it did not fire anywhere in 30k real pixels. Implementing rebasing would add an approximation gated behind `quality` in exchange for nothing measurable, so it is deferred until a view is found that glitches | deferred |

| 37 | **A perturbation kernel on the GPU, so deep views are not CPU-only.** WGSL carries the delta as a floatexp with an `f32` mantissa and an `i32` exponent — a delta spans 2^-1000 to 1 in one run — against a reference orbit computed once on the CPU at full precision and uploaded as `(re.m, im.m, re.e, im.e)`. A pixel the fast path cannot trust (Pauldelbrot's criterion) is flagged, not guessed: the host recomputes it with the exact direct engine and the stage says `+repair`. | `src/domain/render/webgpu-shader.ts` (`WGSL_PERTURB`), `webgpu.ts` | Before this the GPU refused _every_ view past 2^-40 and the app fell back to the CPU pool, so "GPU-first" was false exactly where zooming gets interesting. Measured against the arbitrary-precision oracle: **192/192 escape counts exact at 2^-24 for budgets from 4 to 400**, and 48/48 at 2^-40, 2^-60 and 2^-100 at both 300 and 2000 iterations. The first version agreed on 10/192 because the step's exponent was written as an integer bit pattern into a `vec2<f32>` field, so the shader read a denormal and truncated it to zero — a budget sweep made that obvious, since no rounding story explains disagreement at _four_ iterations | accepted |
| 38 | **WebGL2 gets the same perturbation engine, so the broad-reach GPU path reaches any depth.** GLSL ES 3.00 carries the delta as a floatexp (`floatBitsToUint`/`uintBitsToFloat`, `int` exponent), the reference orbit lives in an `RGBA32F` **texture** (`texelFetch`, row-major over a uniform width) because WebGL2 has no storage buffers, and the bottom-up `gl_FragCoord` convention is handled explicitly. The flag-and-repair path is _shared_ with WebGPU (`repair.ts`), so a refusal means one thing on both APIs. | `src/domain/render/webgl2.ts`, `repair.ts` | WebGL2 is the only GPU path most browsers have (no WebGPU in Safari or on most mobile) and it has neither `f64` nor a double-single kernel, so it could not render past 2^-20 at all. Measured: **192/192 exact** at 2^-24 with every pixel escaping. It does _not_ add emulated double precision — that came later (row 39) — and `exact` at depth stays with the CPU | accepted |
| 39 | **Emulated double precision on WebGL2, built without `fma`.** The exact product WGSL gets from one `fma` is built by **Veltkamp splitting** (each operand split into 12-bit halves whose products are exact in `f32`), and the kernel carries its own header with **four** split uniforms because a `vec2` holds one `(hi, lo)` pair and there are two axes. | `src/domain/render/webgl2.ts` (`DS_HELPERS`), `doubleSingle.ts` | Closes the last named piece of objective item 2: both GPU backends now have an `exact` path to 2^-40. Measured: a boundary view with **19 distinct escape counts and 0 mismatches**, and a 2^-24 view that plain f32 cannot express with **192/192 exact**. Two bugs were caught by pins rather than review — sharing the L0 header made both axes read the same uniform (a collapsed render looks exactly like a uniform view, so the _non-degeneracy_ assertion is what caught it), and selecting the kernel by depth before quality sent `exact` at 2^-24 to the preview-only perturbation shader | accepted |
| 40 | **The delta kernel runs two pixels per instruction (f64x2).** Each lane carries one pixel's floatexp delta; everything derived from the reference — `Z_n`, `2*Z_n`, `|Z_n|^2`, the glitch threshold — is computed once per pair and broadcast, and the scalar path stays as the tail for an odd count or a differing start iteration. | `crates/perturb/src/lib.rs` | Objective item 3 asks for a SIMD core and the kernel was scalar. Measured back to back on one view: scalar **172-205ms**, paired **77-95ms** — about **2x on the kernel** — and **7.2-8.5x** against the JavaScript carrier, up from 3.3x when the kernel landed. Bit-identical on every differential. Four bugs, all found by _isolation scenarios_ rather than by reading: unsigned `i64x2` comparisons of negative exponents, reversed `v128_bitselect` polarity in `fe_add`, exhaustion tested with only the glitch mask, and a complex multiply using raw lane subtraction instead of the floatexp alignment step | accepted |
| 41 | **BLA moves into the WASM kernel: the jump loop runs in Rust against tables composed on the host.** `iterate_batch_bla` applies `d <- A(n,2^j)*d + B(n,2^j)*dc`, checks escape and glitch at block boundaries and re-iterates the escaping block exactly. The coefficients are still built in JavaScript and uploaded, so both carriers apply _identical_ values and can be held to bit-identity; the kernel reports `blocks` and `refined` so replacing the JavaScript carrier loses no observability. | `crates/perturb/src/lib.rs`, `perturbWasm.ts`, `cpu.ts` | The last named algorithm was still JavaScript, and f64x2 had narrowed its margin over the exact kernel to ~1.15x. Measured against the JavaScript carrier applying the same tables: **8.34x** on an escaping view (10^-24, 192/192 escaped and all refined) and **7.88x** on a deep interior view (10^-15); probes at other depths measured 13-49x. One bug found by the differential: JavaScript reports `refined: true` whenever it _re-iterates_ a jumped block, even when the escape is not found inside it | accepted |
| 42 | **The jump loop is paired too, so the SIMD core covers both engines.** Two pixels advance together through a block, the table read and the reference value are shared, and each lane's last block rides in vectors because a lane that escapes stops advancing while the other keeps jumping. The exact re-iteration of an escaping block stays scalar. | `crates/perturb/src/lib.rs` (`iterate_bla_pair`) | Measured on a jump-dominated view: **5.4ms -> 3.6ms, about 1.5x**, and 9.64x against JavaScript. On a _refinement_-dominated view the time is **unchanged** (5.9 -> 6.4ms, within noise) because those pixels are in the scalar re-iteration — reported rather than averaged away. Bit-identical on both, including `blocks` and `refined`. Crossover check in the transition region where only short blocks validate (2^-20, 2^-15): preview 8.8-11.6ms against the exact kernel's 16.7-18.4ms, zero differing counts | accepted |
| 43 | **A tile is written straight into the pass image, so the pixels never cross a thread boundary.** The scheduler allocates the pass image in a `SharedArrayBuffer` when the page is isolated and hands each tile the byte offset and stride of its rectangle; a worker that can write there reports `writtenToTarget` and the scheduler skips its blit. When a tile cache needs the pixels for reuse, the _tile's_ buffer is shared instead, which removes the pool's scratch copy without changing cache semantics. | `backend.ts` (`TileTarget`, shared `makeTileResult`), `scheduler.ts` (`allocatePassPixels`, `tilesDirect`), `workerPool.ts`, `worker/tile.worker.ts` | The last named piece of objective item 3: `SharedArrayBuffer` was carrying tiles, but the pass image was assembled by two main-thread copies per tile. Pinned: a colour view through four workers is identical to the serial backend's and reports `tilesDirect === tilesRendered`, with isolation asserted. The wiring exposed a latent bug — the worker's blit sized its view `rows * stride`, correct only for a tile-sized buffer at offset 0, which overran for any rectangle near the image's right edge | accepted |
| 44 | **The ladder is told what a render measured, so it can select its own middle rung.** A backend records the series prefix it actually validated against a view _shape_ (scale, canvas, budget) and the planner reads it on the next plan for the same view. | `src/domain/ladder/measurements.ts`, `cpu.ts` (records), `App.tsx` (reads) | `planView` had priced `measuredSeriesSkip` since the ladder landed and **nothing ever supplied one**, so the series stage was priced as if it skipped nothing and was never chosen — a rung that could not be climbed, with every test of the pricing arithmetic passing throughout. Measured end to end: on a 10^-60 view the first plan is `perturbation` (work 174,823), a preview render validates **399 of 400 iterations** and records it, and the next plan is `perturbation-series` with work **4,477 — 39x lower**. Pinned as a _loop_ (plan, render, re-plan) rather than as arithmetic, and keyed on the shape so views a pixel apart share what was measured | accepted |
<!-- Rows 35 and 36 were never assigned: the numbering jumped from 34 to 37 when the GPU work landed. -->

| 45 | **Orion deploys to futuremagic.de the way the sibling projects do, with the step the Linux path skipped.** `deploy-sync.sh` builds at base `/Orion/`, patches `RewriteBase` into `public/.htaccess`, diff-syncs `dist/` over FTP (never wiping the remote), and then **registers** the app: `scripts/futuremagic-registry.py` downloads `/webseiten/apps.json`, upserts one entry (`slug`, `title`, `path`, `updatedAt`, `manifesto`) and writes it back. The website renders a card per entry and fetches `<path>/futuremagic.json` for the title, tagline, tags and screenshot. | `deploy-sync.sh`, `scripts/deploy-ftp.py`, `scripts/futuremagic-registry.py`, `public/.htaccess`, `public/futuremagic.json`, `public/shot.png`, `scripts/make-screenshot.mjs` | Registration existed only in the Windows helper (`Register-FuturemagicApp.ps1`, which the Linux deploy printed `[SKIP]` for), so a Linux deploy left the app unlisted. Derived from the live artefacts rather than guessed: the registry's schema and entry order from `/webseien/apps.json`, the card's fields from the site bundle, and the screenshot rule from the site's own URL builder — a **relative** screenshot resolves inside the app's directory, which is why the sibling's absolute `/shots/...` path is a 404 and Orion's `shot.png` is not. The upsert **reproduces the Windows helper's bytes exactly** (CRLF, its colon and bracket alignment), verified by round-tripping the live registry byte-for-byte, so a registration from here produces a diff containing one entry instead of reformatting a file eleven other apps depend on. Safety: `--dry-run`, a local backup of the registry before every write, an offline `--selftest` wired into the gate, and a pin on the deploy's contract (wasm MIME, both isolation headers, and that the registered path equals the deployed one) | accepted |

| 46 | **The app fills the window through one presentation seam and one view-resize seam, with no new dependencies.** A stylesheet of CSS custom properties (`tokens.css`) owns every chrome value; `AppShell` is the only layout (top bar / edge-to-edge viewport / status bar); the canvas backing store is sized from the measured viewport bounded to a 2M-device-pixel budget at `min(devicePixelRatio, 2)`; and `resizeView(view, w, h)` re-expresses the view at a new pixel grid by preserving the complex-plane **centre bit-exactly and the per-pixel scale**, so a window resize shows more or less plane without changing the place or the zoom. Existing product interactions are untouched and the status/centre strings stay single text nodes. | `src/styles/tokens.css`, `src/styles/global.css`, `src/ui/AppShell.tsx`, `src/ui/ErrorBanner.tsx`, `src/ui/useElementSize.ts`, `src/ui/backingScale.ts`, `src/domain/view/view.ts`, `src/App.tsx`, `public/shot.png` | Root cause was measurable: no CSS at all (browser default body margin plus a content-sized `<main style="padding:16">`) and a hard-coded 960x640 canvas constrained only by `maxWidth:100%`, which caps but never grows. The budget is deliberate rather than a plain DPR multiply: a full-window canvas at DPR 2 is ~4x the pixels, and this project documents deep renders at tens of seconds, so the scale is bounded instead of silently multiplied. Pinned: `resizeView` preserves centre and zoom depth and rejects bad dimensions; `backingScale` honours the budget and clamps to [1, min(dpr,2)]; the **browser lane is 41/41 with every existing selector unchanged**, which is what proves the workover did not move the behavioural contract. Evidence that the pins bite: the first wiring left the scratch canvas visible because `.orion-viewport canvas` out-specifies `.orion-scratch`, so `canvas:visible` matched two elements and the app-boot and wheel tests failed in strict mode. Caveat stated rather than hidden: this container has no system Chrome, so the lane was run against Playwright's managed Chromium through a temporary config; the repo's `channel: "chrome"` and known "browser version not pinned" debt are unchanged | accepted |

| 47 | **The browser lane drives Playwright's managed Chromium, so the gate is reproducible and the browser is version-pinned.** `playwright.config.ts` uses `channel: "chromium"`; the revision is pinned by `playwright@1.63.0` in `pnpm-lock.yaml`. `scripts/setup-browser.sh` installs it into a checkout-relative persistent directory and, on a rootless Debian image, stages the shared libraries its build links against; the generated `.browser-env.sh` (gitignored) is sourced by `scripts/with-browser-env.sh` for `test:browser` and `screenshot`. | `playwright.config.ts`, `scripts/setup-browser.sh`, `scripts/with-browser-env.sh`, `package.json`, `scripts/make-screenshot.mjs`, `.gitignore` | The "browser version not pinned" debt stopped being theoretical: landing 46's container had no system Chrome, `channel: "chrome"` searches only root-owned paths (`/opt/google/chrome`, `/usr/bin/google-chrome*`), and the gate could not launch at all. Measured: managed Chromium 153.0.8010.12 (revision 1243) passes **all 41 browser tests** including the WebGL2/WebGPU correctness differentials, and the **full `scripts/gate.sh` is green** here with no manual environment and no root. The setup is idempotent (second run is a no-op). Rejected: installing Google Chrome (needs root) and a per-shell `LD_LIBRARY_PATH` export (not reproducible). Honest limitation: the library staging is Debian-specific; a non-Debian rootless image must install the deps itself | accepted |

| 48 | **A push to `main` auto-deploys Orion, and CI runs the repo's gate before the same `deploy-sync.sh` the manual path uses.** One workflow, `.github/workflows/deploy.yml`: checkout → pnpm/node → install → Rust wasm target → managed browser → `bash scripts/gate.sh` → `bash deploy-sync.sh`. `FTP_PASSWORD` is a repository secret; a concurrency group stops overlapping syncs. The gate's wasm drift check is byte-exact, so `rust-toolchain.toml` pins rustc **1.98.1** (the version that produced the committed artifacts) for local `cargo` and CI alike. | `.github/workflows/deploy.yml`, `rust-toolchain.toml`, `README.md` | The sibling project has an Actions deploy that uploads with `SamKirkland/FTP-Deploy-Action`, but that path does not do Orion's two load-bearing steps — patch `RewriteBase /Orion/` into the dotfile Vite refuses to copy, and upsert the registry entry whose bytes must stay compatible with the Windows helper — so reproducing it in YAML would create a second deploy path that can drift. Wrapping `deploy-sync.sh` instead makes CI and a local deploy the same code. The workflow runs the gate first (Rust + managed Chromium installed for it), so a build that fails wasm-drift/typecheck/lint/unit/browser is never published; the sibling ships untested. The toolchain pin was added because a floating `stable` would rebuild the wasm with a different rustc and fail the byte comparison, blocking every deploy — a false alarm, not a real drift. Verified here: workflow YAML parses, and the pinned toolchain still passes `check-wasm`; it has **not** been run on GitHub, and the `FTP_PASSWORD` secret cannot be created or checked without GitHub auth | accepted |

<!-- Next free row: 49 — dispatcher (or solo agent) assigns the number in the brief -->
